#pragma once

#include <algorithm>
#include <vector>

using std::sort;
using std::vector;

namespace Math {
	double VidurkioSkaiciavimas(vector<int>&);
	double MedianosPaieska(vector<int>&);
}