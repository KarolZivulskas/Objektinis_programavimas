#pragma once

#include <algorithm>
#include <vector>

using std::vector;

namespace Math {
	double VidurkioSkaiciavimas(vector<int>&); //findMean
	double MedianosPaieska(vector<int>&);//findMedian
}